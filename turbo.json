{
    "$schema": "https://turbo.build/schema.json",
    "ui": "tui",
    "tasks": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["dist/**"]
        },
        "lint": {
            "dependsOn": ["^lint"],
            "env": ["RPC"]
        },
        "typecheck": {
            "dependsOn": ["build", "^build", "^typecheck"]
        },
        "test": {
            "dependsOn": ["build", "^build"],
            "outputs": []
        }
    }
}
